# SERVER CONFIGURATION
server.port=8080
server.servlet.context-path=/
server.servlet.session.timeout=30m
server.error.include-stacktrace=never
server.error.include-message=always

# DATABASE CONFIGURATION
spring.datasource.url=jdbc:mysql://localhost:3306/resolveit?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=Naveen@61892842
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# JPA/HIBERNATE CONFIGURATION
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.generate_statistics=false

# FILE UPLOAD CONFIGURATION
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.enabled=true

# JWT CONFIGURATION
jwt.secret=B2dhS8+PII2Q0IuU7Z842r9/mjHCgKuAvWtJvNbo3H4=
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# SECURITY/CORS CONFIGURATION
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173
app.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
app.cors.allowed-headers=*
app.cors.allow-credentials=true

# EMAIL CONFIGURATION (GMAIL SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=naveenproject07@gmail.com
spring.mail.password=driadpfkyocsjqnw
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.default-encoding=UTF-8

# LOGGING CONFIGURATION
logging.level.com.resolveit=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.file.name=logs/resolveit.log
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# APPLICATION SPECIFIC CONFIGURATION
app.name=ResolveitApp
app.version=1.0.0
app.description=Complaint Management System

# Export configuration
export.directory=exports
export.max-records=10000

# Cache configuration
spring.cache.type=simple
spring.cache.cache-names=complaints,users,exports

# Async configuration
spring.task.execution.pool.core-size=5
spring.task.execution.pool.max-size=10
spring.task.execution.pool.queue-capacity=100
spring.task.execution.thread-name-prefix=resolveit-async-

# SPRING ACTUATOR (OPTIONAL - FOR MONITORING)
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always
management.info.env.enabled=true

# Escalation Configuration FOR TESTING
# CHANGE: Using minutes instead of days for testing
escalation.threshold.minutes=7
escalation.check.cron=0 */1 * * * ?

# Enable scheduling
spring.task.scheduling.enabled=true